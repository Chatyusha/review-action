on: [push]

jobs:
  test:
    runs-on: sirius02
    name: test job
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: master
      - name: Test Review Build Job
        uses: ./ # Uses an action in the root directory
        with:
          path: sample
